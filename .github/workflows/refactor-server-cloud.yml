name: One-shot server cloud migration

on:
  push:
    branches:
      - refactor/lore-atlas-v2
    paths:
      - scripts/migrate-server-cloud.mjs
      - tests/sigv4.test.mjs
      - .github/workflows/refactor-server-cloud.yml

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/lore-atlas-v2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Prepare migration
        run: node scripts/migrate-server-cloud.mjs
      - name: Parse and focused architecture checks
        run: |
          node --check server.js
          node --check server/cloud-transports.js
          node --check server/cloud-service.js
          node --check server/cloud-routes.js
          node --test tests/server-cloud-transports.test.mjs tests/server-cloud-service.test.mjs tests/server-cloud-routes.test.mjs tests/server-cloud-boundary.test.mjs tests/sigv4.test.mjs
      - name: Full test suite
        run: npm test
      - name: Commit migration and remove one-shot scaffolding
        run: |
          rm scripts/migrate-server-cloud.mjs
          rm .github/workflows/refactor-server-cloud.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server.js server/cloud-transports.js server/cloud-service.js server/cloud-routes.js tests/server-cloud-transports.test.mjs tests/server-cloud-service.test.mjs tests/server-cloud-routes.test.mjs tests/server-cloud-boundary.test.mjs scripts/migrate-server-cloud.mjs .github/workflows/refactor-server-cloud.yml
          git commit -m "refactor(server): extract cloud sync"
          git push origin HEAD:refactor/lore-atlas-v2
