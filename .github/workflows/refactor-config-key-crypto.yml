name: One-shot config key crypto migration

on:
  push:
    branches:
      - refactor/lore-atlas-v2
    paths:
      - scripts/migrate-config-key-crypto.mjs
      - .github/workflows/refactor-config-key-crypto.yml

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/lore-atlas-v2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: node scripts/migrate-config-key-crypto.mjs
      - name: Parse and architecture checks
        run: |
          node --check public/app.js
          node --check public/modules/app/config-key-crypto.js
          node --test tests/config-key-crypto.test.mjs tests/config-key-crypto-boundary.test.mjs
      - name: Run npm test
        run: npm test
      - name: Commit migration and remove one-shot scaffolding
        run: |
          git config user.name "linhony123654"
          git config user.email "105883346+linhony123654@users.noreply.github.com"
          git add public/app.js tests/config-key-crypto-boundary.test.mjs
          git rm scripts/migrate-config-key-crypto.mjs .github/workflows/refactor-config-key-crypto.yml
          git commit -m "refactor(app): delegate config key crypto protocol"
          git push origin HEAD:refactor/lore-atlas-v2
