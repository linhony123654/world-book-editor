name: One-shot server AI proxy migration

on:
  push:
    branches:
      - refactor/lore-atlas-v2
    paths:
      - scripts/migrate-server-ai-proxy.mjs
      - .github/workflows/refactor-server-ai-proxy.yml

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/lore-atlas-v2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Prepare migration
        run: node scripts/migrate-server-ai-proxy.mjs
      - name: Parse and focused architecture checks
        run: |
          node --check server.js
          node --check server/ai-proxy-service.js
          node --check server/ai-proxy-routes.js
          node --test tests/server-ai-proxy-service.test.mjs tests/server-ai-proxy-routes.test.mjs tests/server-ai-proxy-boundary.test.mjs
      - name: Full test suite
        run: npm test
      - name: Commit migration and remove one-shot scaffolding
        run: |
          rm scripts/migrate-server-ai-proxy.mjs
          rm .github/workflows/refactor-server-ai-proxy.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server.js server/ai-proxy-service.js server/ai-proxy-routes.js tests/server-ai-proxy-service.test.mjs tests/server-ai-proxy-routes.test.mjs tests/server-ai-proxy-boundary.test.mjs scripts/migrate-server-ai-proxy.mjs .github/workflows/refactor-server-ai-proxy.yml
          git commit -m "refactor(server): extract AI proxy"
          git push origin HEAD:refactor/lore-atlas-v2
