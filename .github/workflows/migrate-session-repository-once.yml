name: One-shot session repository migration

on:
  push:
    branches:
      - refactor/lore-atlas-v2
    paths:
      - scripts/migrate-session-repository.mjs

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/lore-atlas-v2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: node scripts/migrate-session-repository.mjs
      - name: Parse and architecture checks
        run: |
          node --check public/modules/chat.js
          grep -q "./ai/session/repository.js" public/modules/chat.js
          if grep -q "let persistQueue = Promise.resolve()" public/modules/chat.js; then exit 1; fi
          if grep -q "fetch('/api/ai-data/'" public/modules/chat.js; then exit 1; fi
      - run: npm test
      - name: Commit migration and remove one-shot scaffolding
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/migrate-session-repository-once.yml scripts/migrate-session-repository.mjs
          git add public/modules/chat.js
          git commit -m "refactor(ai): delegate session persistence repository"
          git push origin HEAD:refactor/lore-atlas-v2
