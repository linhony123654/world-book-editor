name: One-shot server AI data migration

on:
  push:
    branches:
      - refactor/lore-atlas-v2
    paths:
      - scripts/migrate-server-ai-data.mjs
      - .github/workflows/refactor-server-ai-data.yml

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/lore-atlas-v2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Prepare migration
        run: node scripts/migrate-server-ai-data.mjs
      - name: Parse and focused architecture checks
        run: |
          node --check server.js
          node --check server/ai-data-service.js
          node --check server/ai-data-routes.js
          node --test tests/server-ai-data-service.test.mjs tests/server-ai-data-routes.test.mjs tests/server-ai-data-boundary.test.mjs
      - name: Full test suite
        run: npm test
      - name: Commit migration and remove one-shot scaffolding
        run: |
          rm scripts/migrate-server-ai-data.mjs
          rm .github/workflows/refactor-server-ai-data.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add server.js server/ai-data-service.js server/ai-data-routes.js tests/server-ai-data-service.test.mjs tests/server-ai-data-routes.test.mjs tests/server-ai-data-boundary.test.mjs scripts/migrate-server-ai-data.mjs .github/workflows/refactor-server-ai-data.yml
          git commit -m "refactor(server): extract AI data persistence"
          git push origin HEAD:refactor/lore-atlas-v2
