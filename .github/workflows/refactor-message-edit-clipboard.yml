name: One-shot message edit and clipboard migration

on:
  push:
    branches:
      - refactor/lore-atlas-v2
    paths:
      - 'scripts/migrate-message-edit-clipboard.mjs'
      - '.github/workflows/refactor-message-edit-clipboard.yml'

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/lore-atlas-v2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: node scripts/migrate-message-edit-clipboard.mjs
      - name: Parse and architecture checks
        run: |
          node --check public/modules/chat.js
          node --check public/app.js
          node --check public/modules/ai/ui/message-edit-view.js
          node --check public/modules/ai/ui/clipboard.js
          node --check tests/message-edit-clipboard-boundary.test.mjs
      - run: npm test
      - name: Commit migration and remove one-shot scaffolding
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f scripts/migrate-message-edit-clipboard.mjs
          rm -f .github/workflows/refactor-message-edit-clipboard.yml
          git add -A
          git commit -m "refactor(ai): delegate message editing and clipboard"
          git push origin HEAD:refactor/lore-atlas-v2
