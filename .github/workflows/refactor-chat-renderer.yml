name: One-shot chat renderer migration

on:
  push:
    branches:
      - refactor/lore-atlas-v2
    paths:
      - 'scripts/migrate-chat-renderer.mjs'
      - '.github/workflows/refactor-chat-renderer.yml'

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/lore-atlas-v2
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: node scripts/migrate-chat-renderer.mjs
      - name: Parse and architecture checks
        run: |
          node --check public/modules/chat.js
          node --check public/app.js
          node --check public/modules/ai/ui/chat-renderer.js
          node --check tests/chat-renderer-boundary.test.mjs
      - run: npm test
      - name: Commit migration and remove one-shot scaffolding
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f scripts/migrate-chat-renderer.mjs
          rm -f .github/workflows/refactor-chat-renderer.yml
          git add -A
          git commit -m "refactor(ai): delegate chat presentation DOM"
          git push origin HEAD:refactor/lore-atlas-v2
